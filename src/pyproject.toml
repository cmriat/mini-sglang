[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minisgl"
version = "0.1.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["minisgl"]